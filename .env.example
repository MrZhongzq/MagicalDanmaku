# 复制成 .env 并改掉密码。
#
# docker compose 会自动读取同目录下的 .env。

# 数据库密码。**必须改掉**，compose 不给默认值就是为了逼你改。
#
# ⚠️ 只能用 URL 安全字符（字母、数字、- _ . ~）。
# 这个密码会被原样拼进连接串 postgres://用户:密码@主机/库，
# 含有 @ : / # ? % 或中文会让连接串解析失败，而报错信息
# （invalid userinfo）与真实原因毫无关系，极难排查。
#
# 生成一个安全又不会踩坑的：
#   openssl rand -hex 24
POSTGRES_PASSWORD=在这里填 openssl rand -hex 24 的输出

# ⚠️ 管理员初始密码。**无头部署的第一道坎，必须在这里设置。**
#
# 库是空的时候（第一次部署）会用这个密码建管理员账户；
# 库里已经有管理员之后，改这个值不会生效——不会覆盖已有密码，
# 也不留改密码的后门。改密码用 `magicd user passwd <用户名>`。
#
# 不设的话 migrate 会直接报错拒绝建库，而不是像旧版本那样生成一个
# 随机密码、只在 `docker compose logs migrate` 里打印一次——
# 无头设备翻这个日志本身就很麻烦，这正是本项设置的初衷。
#
# 至少 8 位，生成一个够强的：
#   openssl rand -base64 18
MAGICD_ADMIN_PASSWORD=在这里填 openssl rand -base64 18 的输出

# 可选：数据库用户名与库名，默认都是 magicd
# POSTGRES_USER=magicd
# POSTGRES_DB=magicd

# 可选：镜像 tag，默认 latest。生产环境建议钉死具体版本。
# MAGICD_TAG=v7.0.0

# 可选：WebUI 的宿主监听地址与端口，默认 127.0.0.1:20992。
#
# 端口取 20992 这样的高位端口是为了避开冲突 —— 8080 是最容易被别的服务
# 占掉的端口之一，而冲突的表现（起不来 / 连到了别人的服务）都不指向真实原因。
# 容器内部固定 8080，改这里只影响宿主侧。
#
# 默认只绑本机回环 —— WebUI 是唯一的管理入口，不该因为「装上就能用」
# 而默认对整个局域网开放。**要从别的机器访问就把 BIND 改成 0.0.0.0**，
# 这也是「服务在跑但网页打不开」最常见的原因。
# MAGICD_HTTP_BIND=0.0.0.0
# MAGICD_HTTP_PORT=8080

# 可选：日志级别 debug / info / warn / error
# MAGICD_LOG_LEVEL=info

# 可选：时区。定时规则按它计算，设错了规则会整体偏移。
# TZ=Asia/Shanghai
