# P4-2 悬空清单

> 用户裁决（2026-08-01）：**后端接口统一最后处理**，先看前台的开关和设置对不对，
> 免得最后前后端一起返工。
>
> 本清单**边做边追加**，每个任务完成时补上自己新增的项。它是后端补丁批次的输入。

## 怎么读

每条列：**所在位置** / **缺什么**（具体到字段、接口或能力）/ **补上之后前端改哪里** /
**是否需要真实样本**。

「需要真实样本」的意思是：控制器无法凭现有信息确定报文形状，必须由用户在真实
直播间触发一次并抓包，否则写出来的解析代码是猜的。

---

## 已登记

### 1. 账号登录状态（账号与直播间页）

- **位置**：账号卡片上的「登录状态」标签
- **缺什么**：后端没有定期检测 B 站登录态的能力。设计文档 §13.1
- **补上之后**：把那个 `NTag` 换成真实状态（有效/已失效/检测中）+ 上次检测时间；
  失效的账号高亮
- **需要真实样本**：否

### 2. 主播区：拉黑与解除拉黑（房管页）

- **位置**：房管页「主播区」整块
- **缺什么**：后端只做了禁言/解禁，没有拉黑接口
- **补上之后**：把 `ownerActionNotSupported()` 换成真实调用
- **需要真实样本**：否

### 3.（Task 14 已解决，不再悬空）自动禁言规则（房管页）

- **位置**：房管页底部「自动禁言规则」卡片的「去配置」按钮
- **原来缺什么**：不算真缺口——它属于「规则」，要靠自定义弹幕姬页的条件构建器
  配置。Task 11 做完 `custom` 路由后按钮真能跳过去了，但「预填自动禁言模板」
  那一半当时没接——按钮到得了地方，做不了标签承诺的事（登记在 Task 11 的
  收尾待办里）
- **Task 14 怎么解决的**：`Moderation.vue` 的 `goToCustomDanmaku` 跳转时带
  `query: { preset: 'automute' }`；`Custom.vue` 用
  `new URLSearchParams(window.location.search).get('preset')`（不用
  `useRoute()`，因为本页大量测试直接 `mount(Custom.vue)` 不挂
  vue-router，`useRoute()` 在没有 router 插件时会抛错）读到
  `preset === 'automute'` 后往 `customRules` 里插入一条已经能跑的骨架：
  `on: ['danmaku']`、`when: { field: 'text', op: 'contains', value: '' }`、
  `do: [{ type: 'block', hours: 1 }]`。关键词故意留空——禁言关键词因人而异，
  编不出默认值，硬塞一个示例词还可能被当成"已经配置好"直接点保存。用
  `presetApplied` 标志保证只应用一次，防止切换直播间导致 `loadRules`
  重新触发时重复插入
- **不属于本条的限制**：这条预填草稿和页面上任何手动新建的规则一样，仍然
  只是本地草稿，要点右上角「保存并生效」才会真正写库生效；切换直播间会
  像丢弃其它未保存草稿一样把它一并丢弃，这是全页统一的既有行为，不是这
  条预填功能独有的缺陷
- **需要真实样本**：否

### 4. 清除业务日志（日志页）

- **位置**：日志页「清除」按钮
- **缺什么**：后端没有删除 `activity_logs` 的接口
- **补上之后**：接上 `DELETE /api/bindings/{id}/activity`（需新增），二次确认
- **需要真实样本**：否

### 5. 模板轮询模式（弹幕姬页：进房欢迎 / 礼物答谢）

- **位置**：两个功能块「欢迎语模板」/「答谢语模板」下的「随机抽取／轮询」单选框
- **缺什么**：`server/internal/rules/template.go` 的 `Renderer.Render` 目前只会
  `rand.Intn` 随机挑一条模板（第 29-39 行），没有轮询需要的游标状态。
  `spec.Action` 也没有对应字段（如 `pick`）来声明这条规则用哪种挑选方式
- **补上之后**：`spec.Action` 加 `Pick` 字段（`random`/`roundrobin`），
  `rules.Rule`/`Renderer` 记住每条规则上次用到第几条模板；前端把
  `Danmaku.vue` 里 `EnterDraft.pickMode`/`GiftDraft.pickMode` 接进
  `buildEnterRule`/`buildGiftRule` 组装的 `Action.pick` 里
- **需要真实样本**：否

### 6. 单人/多人两套模板（弹幕姬页：进房欢迎）

- **位置**：「欢迎语模板」下「多人合并欢迎语」整栏（含输入框与「待后端支持」标签）
- **缺什么**：`spec.Action` 只有一个 `Template` 字段，一条规则装不下两套模板；
  合并结算时（`count==1` 单人 vs `count>1` 多人）没有分别取模板集的能力
- **补上之后**：`spec.Action` 加类似 `TemplateGroup`/`MultiTemplate` 的字段，
  `rules` 执行侧按 `Trigger.Vars["count"]` 选用；前端把 `Danmaku.vue` 里
  `EnterDraft.multiTemplates` 接进 `buildEnterRule` 的组装逻辑（目前那里的
  注释写着「多人模板整栏渲染但不参与组装」，就是接这里）
- **需要真实样本**：否

### 7. 盲盒礼物单列 + 盲盒盈亏统计（弹幕姬页：礼物答谢）

- **位置**：「盲盒」小节的两个复选框：「盲盒礼物单独一类，不并入常规答谢」
  「盲盒盈亏统计」
- **缺什么**：`event.Gift`（`server/internal/event/payload.go` 第 19-27 行）完全
  没有盲盒字段。B 站 `SEND_GIFT` 报文里的 `blind_gift` 对象（含
  `original_gift_id`、`original_gift_name`、`gift_action` 等）还没解析
- **补上之后**：`event.Gift` 加 `BlindBox *BlindBox` 字段，`cmdmap` 的
  `SEND_GIFT` 映射解析该字段，规则条件/模板变量里提供 `gift.isBlindBox` 之类
  的谓词；前端把 `Danmaku.vue` 里 `GiftDraft.blindBoxSeparate`/
  `blindBoxProfitTracking` 接进真实的规则字段或独立规则
- **需要真实样本**：**是。** 且这一条不是「随便刷一次就行」——盈亏统计还有
  用户记忆文件里记着的坑：**必须按电池数量统计而非礼物名，且滚动合并时盈亏
  要跟着重算**，这两点都要靠真实报文验证字段形状之后才能确认怎么解析

### 8.（新发现，不在原定四处悬空之列）礼物答谢模板缺「多礼物名合并」变量

- **位置**：礼物答谢的「答谢语模板」输入框下方的可用变量提示
- **缺什么**：设计文档 §7.2 给的示例模板是
  `感谢 {{users}} 的 {{gifts}} 等，……`，但 `{{gifts}}`（本轮合并的多种礼物名
  列表）在后端没有对应变量。`server/internal/rules/aggregate.go` 的
  `mergeBuckets`（第 199-226 行）只显式收集了 `users` 列表，`vars["gift"]`
  只保留合并窗口里**第一个**桶的礼物信息——多人多礼物一起送礼、走
  `by: type` 合并时，`gift.name` 只会是第一件礼物的名字，其余礼物名会丢失
- **补上之后**：`mergeBuckets` 仿照 `users` 的做法，额外收集一份去重后的
  礼物名列表写进 `vars["gifts"]`；前端把 `Danmaku.vue` 里
  `TEMPLATE_VAR_HINT.gifts` 的提示从「后端没有对应变量」改成真正可用，
  并把默认模板从单数 `{{.gift.name}}` 换成 `{{join .gifts "、"}}`
- **需要真实样本**：否——这不是协议解析问题，是合并阶段的聚合逻辑缺失，
  从现有的 `event.Gift`/`Trigger.Vars` 就能实现，不需要抓包

### 9. 模板轮询模式（弹幕姬页：轮播消息）

- **位置**：「轮播消息」块「轮播内容」下的「随机抽取／轮询」单选框
- **缺什么**：与第 5 条同一个病根——`server/internal/rules/template.go` 的
  `Renderer.Render` 只会 `rand.Intn` 随机挑一条，`spec.Action` 没有 `pick`
  字段。第 5 条登记的是进房欢迎/礼物答谢，这里是同一个缺口在第三个功能块
  （轮播消息）上的复现
- **补上之后**：与第 5 条一起补，`Danmaku.vue` 里 `BroadcastDraft.pickMode`
  接进 `buildBroadcastRule` 组装的 `Action.pick`
- **需要真实样本**：否
- **额外提醒**：这个缺口还带来一个命名误导——多条轮播模板目前实际上是
  「随机播」，不是名字暗示的「按顺序轮」。`Danmaku.vue` 里这句话直接写在
  正文（不是塞进 tooltip）：「『轮播』这个词目前名不副实：多条模板一律
  随机抽取一条播出，不是按顺序循环」

### 10. PK 匹配信息（弹幕姬页：PK 播报）

- **位置**：`web/src/components/PkPanel.vue`「PK 匹配信息」小节：四个勾选
  （对面主播昵称/直播间人数/大航海总数/大航海在线数）+ 播报模板
- **缺什么**：`event.Battle`（`server/internal/event/payload.go` 第
  119-121 行）只有一个 `SubCommand` 字符串字段，PK 接通瞬间的对面数据
  协议层完全没解析；`rules.VarsFromEvent`（`server/internal/rules/vars.go`
  第 87-88 行）对应给模板的变量也只有 `battle.subCommand` 一项
- **补上之后**：`event.Battle` 需要新增对面主播昵称/房间人数/大航海总数/
  在线数等字段，`cmdmap` 解析 `PK_BATTLE_START_NEW`/`PK_BATTLE_PRE_NEW`
  等 CMD 的完整报文，`VarsFromEvent` 把新字段展开成
  `battle.opponentXxx`；前端把 `PkPanel.vue` 里 `matchTemplates` 用到的
  占位字段名换成真实字段名，四个 `announceXxx` 勾选决定播报内容这件事
  也要在那时候重新设计（目前完全不参与组装）
- **需要真实样本**：**是。** 且这条不是随便找一次 PK 就行——报文形状、
  字段在第几层都要靠真实抓包确认，控制器凭现有信息编不出来。字段名
  `battle.opponentName` 等目前都是占位示例，很可能与最终形状不同

### 11. PK 串门欢迎（弹幕姬页：PK 播报）

- **位置**：`web/src/components/PkPanel.vue`「PK 串门欢迎」小节：开关 +
  欢迎语模板
- **缺什么**：比第 10 条缺得更彻底——连"这个进场事件来自对面直播间"这件事
  本身该怎么判断都还没确认。`event.UserEnter` 没有任何来源房间信息，
  可能需要一个新的事件类型而不只是补字段
- **补上之后**：待 PK 报文抓包分析后确定判断方式（可能是新增
  `event.UserEnter.FromRoomID` 之类的字段，也可能需要单独的事件类型），
  再决定这条规则该挂在哪个 `on` 上；`PkPanel.vue` 的 `visitGreetingEnabled`/
  `visitTemplates` 目前完全不写进任何规则，纯粹是给评审看设想的形状
- **需要真实样本**：**是**，且这条甚至定不下来该走哪条技术路线，
  抓包之后可能还要再补一版设计

### 12.（Task 11 新增）变量清单没有对应的元数据接口

- **位置**：`web/src/components/ConditionTree.vue` 叶子条件的「字段」下拉
  （`COMMON_FIELD_OPTIONS`），`Custom.vue` 把它作为 `field-options` 传给
  `ConditionTree`
- **缺什么**：`GET /api/meta/event-types`/`action-types`/`operators`/
  `aggregate-by` 四个元数据接口都有（`server/internal/httpapi/server.go`
  第 137-141 行），唯独没有 `GET /api/meta/variables`。条件的 `field` 是
  点分路径（`user.uid`、`user.medal.isLighted` 这些），理论上应该跟其它
  四份清单一样从后端下发，但后端压根没开这个口
- **补上之后**：`server/internal/rules/vars.go` 的 `VarsFromEvent`/
  `userVars` 是唯一权威来源，新增一个 `GET /api/meta/variables` 接口把
  它实际展开的路径（连同各自适用的事件类型、大致含义）序列化下发；
  前端把 `ConditionTree.vue` 里手写的 `COMMON_FIELD_OPTIONS`
  （20 条，照抄自读 `vars.go` 源码的结果）换成从这个接口拉取，删掉这份
  硬编码
- **需要真实样本**：否——这是纯粹的元数据缺口，`vars.go` 本身就是权威
  来源，不需要抓包，只是目前没有一个接口把它序列化出来。**这一条很
  重要**：`COMMON_FIELD_OPTIONS` 现在就是「第二处定义」，`vars.go`
  改字段名/加字段，这份清单不会跟着变，会悄悄漂移——只是眼下没有别的
  办法（`NSelect` 开了 `filterable tag`，用户可以直接打字输入任意路径，
  不受这份清单限制，漂移的后果被这一点缓解了，但没有消除）

### 13.（Task 11 新增）「排除通用规则」——规则引擎缺互斥/优先级机制

- **位置**：`Custom.vue` 每条自定义规则卡片的「排除通用规则」小节，一个
  多选框，列出 Task 9/10 建立的七个内置规则名（进房欢迎/礼物答谢/PK
  播报/轮播消息/关注答谢/分享答谢/上舰答谢）
- **缺什么**：设计文档 §7.2 明确要求"一条自定义规则命中后，可以声明屏蔽
  掉哪些通用功能"，典型场景是"给某位舰长配了专属进房欢迎，就不该再触发
  通用进房欢迎——否则那位舰长进房会被欢迎两次"。`server/internal/rules`
  的执行引擎里各条规则相互独立求值、互不感知，没有"一条规则命中后跳过
  另一条"这种互斥/优先级机制；`spec.Rule`（`server/internal/rules/spec/
  spec.go`）也没有字段能装这份声明
- **补上之后**：需要在引擎侧新增"命中后跳过指定规则"的能力——大致方向
  是 `spec.Rule` 加一个类似 `SuppressRules []string` 的字段，引擎在同一
  批事件分发时记录本轮已命中且带有该字段的规则，对同一事件的其余规则
  跳过名单内的几条；前端把 `Custom.vue` 里 `CustomRuleDraft.
  excludeBuiltinRules` 接进 `buildCustomRule` 组装的 `SuppressRules` 里
  （目前这个字段存在于草稿里、界面能选能看，但完全不参与
  `buildCustomRule` 的组装，写了也没有字段能装）
- **需要真实样本**：否——这是引擎能力缺口，不是协议解析问题，不需要
  抓包，需要的是设计一套互斥/优先级语义并在 `rules` 包里实现

### 14.（Task 12 新增）统计页整体缺聚合接口

- **位置**：`web/src/pages/Stats.vue` 全部统计卡片（弹幕数/进房人数/礼物种类/
  礼物数量/上舰数/直播时长/盲盒盈亏），以及页面顶部的维度切换（按场次/按日）
- **缺什么**：后端目前只有 `GET /api/bindings/{id}/activity`
  （`server/internal/httpapi/activity_handler.go` 第 18 行
  `maxActivityLimit = 500`），单次最多返回 500 条原始行，不做任何聚合。
  一个活跃房间一天的行数远超 500——在前端把这 500 条当全量数字加总展示，
  会给出一个**错误的数字而不告诉任何人**（比如「今天弹幕 500 条」实际只是
  被截断的采样），所以本页所有统计卡片目前一律显示占位符 `—`，不读取
  `/activity` 的返回值去计算任何数字
- **补上之后**：新增 `GET /api/bindings/{id}/stats?by=day|session`（或类似
  路径），由 SQL 对相关事件/日志表做 `GROUP BY` 聚合出：弹幕数、进房人数
  （去重口径待定）、礼物种类与数量、上舰数、直播时长。**直播时长的根因
  不是"缺一张场次表"**——`live_start`/`live_stop` 在 `server/internal/
  event/type.go` 里本来就存在，只是没被列进 `server/internal/logging/
  sink.go` 的 `loggedEventTypes` 白名单（该 map 目前只有 10 个类型），
  所以这两个事件从未写进 `activity_logs`。补丁是往这个 map 里加两行，
  不是新建一张表加迁移——这两种方案的成本差一个数量级，写后端补丁时
  务必先看这里，不要重新设计一张「场次」表。前端把 `Stats.vue` 里
  `STAT_CARDS` 渲染 `PLACEHOLDER` 的地方换成真实请求返回的数字，按
  `dimension`（`day`/`session`）传参给新接口
- **需要真实样本**：否——这是接口缺失，不是协议解析问题

### 15.（Task 12 新增）盲盒盈亏统计（统计页，双重悬空）

- **位置**：`Stats.vue`「盲盒盈亏」卡片（页面上标了「双重悬空」标签）
- **缺什么**：**两层都缺，缺一不可**——① 与第 14 条一样缺聚合接口；②
  与第 7 条一样，`event.Gift` 完全没有盲盒字段，`SEND_GIFT` 报文里的
  `blind_gift` 对象还没解析。补上第 14 条的聚合接口也算不出盈亏，因为
  算盈亏所需的原始数据（开出了什么、花了多少）本身就还没进库
- **补上之后**：先补第 7 条（解析 `blind_gift`，含电池消耗信息），再补
  第 14 条的聚合接口把盈亏按场次/按日汇总。**用户在项目记忆里对这一条有
  明确要求**：盈亏必须按**电池数量**统计，不能按礼物名统计（同一电池
  消耗量在不同盲盒池可能对应不同的开出礼物，礼物名不是稳定的价值锚点，
  按礼物名统计会把同一次消耗的盈亏算错）；且弹幕姬礼物答谢页「滚动合并」
  结算时，如果一个合并窗口里出现多次盲盒开出，盈亏要跟着**同步重算**，
  不能只在合并窗口结束时按快照算一次——这两点补后端时都要照做，不是
  可选的优化项
- **需要真实样本**：**是**——与第 7 条同一个样本需求，不需要重复抓包，
  两条登记的是同一份缺失协议数据在两处不同消费方上的体现（弹幕姬答谢
  文案 vs 统计页盈亏数字）

---

## 勘误：一条「以为悬空、实测已通」不计入本清单

Task 9 简报原话是「`user_enter` 事件 Payload 没有粉丝牌字段」，要求把「进房
欢迎 - 只欢迎佩戴粉丝牌的用户」标成待后端支持。经核对 Go 代码后发现这个说法
不成立：

- `event.User.Medal *Medal`（`server/internal/event/user.go`）本来就带
  `IsLighted`/`RoomID`/`Level`/`GuardLevel` 字段
- `cmdmap.mapInteractWordV2`（`server/internal/connector/bilibili/cmdmap/interactv2.go`
  第 115-127 行）在解析 `INTERACT_WORD_V2` 时会把这些字段填进
  `event.UserEnter.User.Medal`
- `rules.VarsFromEvent`/`userVars`（`server/internal/rules/vars.go` 第
  96-119 行）把它们展开成 `user.medal.isLighted`/`user.medal.roomId`/
  `user.medal.level`
- `rules.evalLeaf`（`server/internal/rules/condition.go`）的 `eq`/`gte`/`in`
  都能直接用这些字段

数据从解析到条件求值全链路已经打通，与设计文档 §13.2 标题本身写的「**已
具备**，需写成规则」一致（§13.2 真正要求补的是一个便利谓词
`user.medal.wearing`，省得前端自己拼 `all: [isLighted, roomId]` 两条，不是
数据缺失）。因此 `web/src/pages/Danmaku.vue` 里「只欢迎佩戴粉丝牌的用户」
筛选是**真实生效**的功能（`buildEnterCondition` 直接拼出对应的 `when` 条件），
没有计入上面的悬空清单；界面上用的是「自动拼条件」信息标签而不是
「待后端支持」警告标签，避免误导评审。
