# P4-2 WebUI 实施计划 · Part 1（Task 1–4：骨架）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 Vue 3 + TypeScript + Vite + Naive UI 实现 P4 设计文档 §7 的八个页面，构建产物 embed 进 Go 二进制。

**Architecture:** SPA，vue-router 管路由，Pinia 管跨页共享状态（当前用户、可见绑定、当前选中的绑定），一层薄的 `api/` 封装 `fetch`。后端接口在 P4-1 已全部就绪（见 P4 设计文档 §5.2）。

**Tech Stack:** Vue 3.5+、TypeScript 5、Vite 6、Naive UI 2、Pinia 2、vue-router 4、Vitest + @vue/test-utils。

## Global Constraints

- **前端源码在仓库根的 `web/`，构建产物输出到 `server/internal/webui/dist/`。** 不要中间的同步步骤——多一个步骤就多一处会忘记执行的地方，忘记的表现是「改了前端却没生效」，很难查
- **构建产物必须提交进仓库。** 这样 `go build` 不需要 node，六平台交叉编译与 GoReleaser 流程完全不变
- **不新增 Go 依赖。** 本阶段只碰 `web/`、`server/internal/webui/dist/`、CI 配置与文档；**不改任何 `.go` 文件**（`webui.go` 已经写好）
- **注释、界面文案、提交信息一律中文。** 变量名与类型名用英文
- **一切改变状态的请求不得用 GET**（后端靠 `SameSite=Lax` 替代 CSRF token）
- **绝不把 B 站 Cookie 显示在界面上。** 后端的 `accountView` 根本不返回它，前端也不该有任何字段去接
- 每个 npm 包都要能在离线 `npm ci` 下装上，不引入需要编译原生模块的依赖（保持 CI 简单）
- TypeScript `strict: true`，`vue-tsc --noEmit` 必须干净
- ESLint 与 Prettier 各一份配置，`npm run lint` 必须干净

## 后端已就绪的接口

全部见 `docs/superpowers/specs/2026-07-31-p4-api-webui-design.md` §5.2。要点：

- 错误统一 `{"error": "中文说明"}`，状态码承载语义
- `401` 未登录、`403` 已登录但无权限、`404` 不存在**或对调用者不可见**、`409` 冲突、`422` 请求体不合法、`502` B 站上游失败、`503` 机器人未在运行
- 会话是 `HttpOnly` Cookie，前端**读不到也不需要读**；`fetch` 必须带 `credentials: 'same-origin'`
- 权限点六个：`rule:read`、`rule:write`、`danmaku:send`、`user:block`、`member:manage`、`event:read`

## 三个贯穿全局的交互决定

### 一、右上角「保存」按钮，改了才亮，按下才生效

用户的明确要求：

> 热重载必须有，但是可以不是实时更改，右上角设置一个保存按键，当设置被更改以后按下保存才生效。

所以**弹幕姬、自定义弹幕姬、房管的自动规则**这几页是**草稿态**：改动只存在前端内存里，右上角的保存按钮从灰变亮；按下后依次
`PUT/PATCH` 写库 → `POST /api/bindings/{id}/reload` → 成功后按钮变灰。

**离开有未保存改动的页面要拦截**（`onBeforeRouteLeave` + `beforeunload`）。

### 二、权限不足时警告，绝不把面板变灰锁死

设计文档 §7.2 页面 3 写死了这条，理由是我们无法可靠预判某账号在某直播间到底有没有房管权限——B 站可能刚给、刚撤，或接口状态滞后。把面板灰掉等于「我判断你没权限所以不让你试」，而这个判断本身可能错。

**统一做法**（不只房管页）：

- 缺权限时页面顶部一条 `n-alert`（`type="warning"`），说明「可能失败」
- 控件**保持可用**
- 操作失败后把后端返回的 `error` 原样显示出来——那句话正是操作者要看的（「你不是本房间的房管」）

**唯一的例外**是「这个绑定对你完全不可见」——那种情况后端返回 404，前端连这个绑定都不该列出来，自然无从操作。

### 三、后端缺口处的控件先悬空

设计文档 §13 列了六个前置缺口（登录态检测、粉丝牌判定口径、模板轮询、盲盒字段、PK 报文、规则排除）。用户裁决是先把界面铺出来，控件在但部分无效，最后一起返工。

**悬空的控件必须有视觉标记**，不能让人以为它生效了：控件旁挂一个 `n-tag`（`size="small"` `type="warning"`）写「待后端支持」，`n-tooltip` 说明缺什么。**不要用 disabled**——那会让人以为是权限问题。

---

## Task 1: 脚手架与构建接线

**Files:**
- Create: `web/package.json`、`web/package-lock.json`、`web/tsconfig.json`、`web/tsconfig.node.json`、`web/vite.config.ts`、`web/index.html`、`web/.eslintrc.cjs`、`web/.prettierrc.json`、`web/.gitignore`
- Create: `web/src/main.ts`、`web/src/App.vue`、`web/src/env.d.ts`
- Create: `web/vitest.config.ts`、`web/src/smoke.test.ts`
- Modify: `.github/workflows/ci.yml`（加前端 job）
- Modify: `.gitignore`（仓库根，确保 `web/node_modules` 被忽略、`server/internal/webui/dist` **不**被忽略）

**Interfaces:**
- Produces: `npm run dev`（开发服务器，代理 `/api` 到 `127.0.0.1:8080`）、`npm run build`（产物到 `server/internal/webui/dist/`）、`npm run lint`、`npm run typecheck`、`npm run test`

- [ ] **Step 1: 建目录与 package.json**

```bash
mkdir -p /f/danmuku/web/src
cd /f/danmuku/web
```

`web/package.json`：

```json
{
  "name": "magicd-webui",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .ts,.vue --max-warnings 0",
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "naive-ui": "^2.40.0",
    "pinia": "^2.2.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.0",
    "@vue/eslint-config-prettier": "^10.0.0",
    "@vue/eslint-config-typescript": "^14.0.0",
    "@vue/test-utils": "^2.4.0",
    "eslint": "^9.10.0",
    "eslint-plugin-vue": "^9.28.0",
    "jsdom": "^25.0.0",
    "prettier": "^3.3.0",
    "typescript": "~5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0",
    "vue-tsc": "^2.1.0"
  }
}
```

**版本号写的是下界，`npm install` 会解出具体版本并锁进 `package-lock.json`。锁文件必须提交。**

- [ ] **Step 2: Vite 配置——产物直接落进 Go 的 embed 目录**

`web/vite.config.ts`：

```ts
import { fileURLToPath, URL } from 'node:url'
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) },
  },
  build: {
    // 直接输出到 Go 的 embed 目录，不做中间同步。
    // 多一个同步步骤就多一处会忘记执行的地方，而忘记的表现是
    // 「改了前端却没生效」，很难查。
    outDir: fileURLToPath(new URL('../server/internal/webui/dist', import.meta.url)),
    emptyOutDir: true,
    // 产物要 embed 进二进制并提交进仓库，关掉 sourcemap 省体积
    sourcemap: false,
  },
  server: {
    port: 5173,
    proxy: {
      // 开发时前端跑在 5173，后端跑在 8080。
      // 必须带 cookie，会话是 HttpOnly Cookie。
      '/api': {
        target: 'http://127.0.0.1:8080',
        changeOrigin: false,
      },
    },
  },
})
```

**`changeOrigin: false` 是有意的**：后端的会话 Cookie 是 `SameSite=Lax`，改写 Origin 会让它在开发环境里表现得和生产不一致，把本该在开发期暴露的问题藏到上线才发现。

- [ ] **Step 3: TypeScript 配置**

`web/tsconfig.json`：

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "jsx": "preserve",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "skipLibCheck": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["src/**/*.ts", "src/**/*.vue", "*.config.ts"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

`web/tsconfig.node.json`：

```json
{
  "compilerOptions": {
    "composite": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": ["node"],
    "noEmit": true
  },
  "include": ["vite.config.ts", "vitest.config.ts"]
}
```

`web/src/env.d.ts`：

```ts
/// <reference types="vite/client" />

declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  const component: DefineComponent<object, object, unknown>
  export default component
}
```

- [ ] **Step 4: 入口文件**

`web/index.html`：

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>神奇弹幕</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

`web/src/main.ts`：

```ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'

createApp(App).use(createPinia()).mount('#app')
```

**路由在 Task 3 接进来**，这一步先让构建跑通。

`web/src/App.vue`：

```vue
<script setup lang="ts">
import { NConfigProvider, NMessageProvider, darkTheme, zhCN, dateZhCN } from 'naive-ui'
</script>

<template>
  <!-- 暗色主题：这是个开播时挂在副屏上的工具，亮色会晃眼 -->
  <NConfigProvider :theme="darkTheme" :locale="zhCN" :date-locale="dateZhCN">
    <NMessageProvider>
      <div class="app-shell">神奇弹幕</div>
    </NMessageProvider>
  </NConfigProvider>
</template>

<style>
html,
body,
#app {
  height: 100%;
  margin: 0;
}
</style>
```

- [ ] **Step 5: 写失败的冒烟测试**

`web/vitest.config.ts`：

```ts
import { fileURLToPath, URL } from 'node:url'
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) },
  },
  test: {
    environment: 'jsdom',
    globals: true,
  },
})
```

`web/src/smoke.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { mount } from '@vue/test-utils'
import App from './App.vue'

describe('App', () => {
  it('挂载后能渲染出应用外壳', () => {
    const wrapper = mount(App)
    expect(wrapper.text()).toContain('神奇弹幕')
  })
})
```

- [ ] **Step 6: 装依赖并跑，确认全链路通**

```bash
cd /f/danmuku/web; npm install 2>&1 | tail -5; echo "退出码=$?"
cd /f/danmuku/web; npm run typecheck 2>&1 | tail -10; echo "退出码=$?"
cd /f/danmuku/web; npm run test 2>&1 | tail -15; echo "退出码=$?"
cd /f/danmuku/web; npm run build 2>&1 | tail -15; echo "退出码=$?"
```

**构建之后必须确认产物真的落在了 Go 的 embed 目录里**，而不是 `web/dist`：

```bash
ls -la /f/danmuku/server/internal/webui/dist/; echo "退出码=$?"
```

应当看到 `index.html` 与 `assets/`。**Task 16 放的那个占位 `index.html` 会被覆盖，这是预期的。**

然后确认 Go 侧仍然能编译并且服务的是新产物：

```bash
cd /f/danmuku/server; go build ./... ; echo "退出码=$?"
cd /f/danmuku/server; go test ./internal/httpapi/ -count=1 -run Static -v 2>&1 | tail -15; echo "退出码=$?"
```

**`TestStaticServesIndex` 这类测试可能断言了占位页里的字符串**（比如「神奇弹幕」或某段占位文案）。如果它红了：

- 断言的是「神奇弹幕」这种真实产物也有的字样 → 应该仍然绿，红了说明产物有问题，查产物
- 断言的是占位页独有的文案（比如「前端尚未构建」）→ **改测试**，改成断言真实产物里稳定存在的东西（`<div id="app">` 或 `<title>神奇弹幕</title>`），并在报告里说明

- [ ] **Step 7: 忽略规则**

`web/.gitignore`：

```
node_modules/
*.local
.vite/
```

仓库根的 `.gitignore` 要确认两件事：`web/node_modules` 被忽略；**`server/internal/webui/dist` 没有被任何规则命中**（它必须提交）。先读一遍现有内容再改，别覆盖已有规则。

```bash
cd /f/danmuku; cat .gitignore; echo "---"; git check-ignore -v server/internal/webui/dist/index.html; echo "退出码=$? (1=没被忽略，正确)"
```

- [ ] **Step 8: ESLint 与 Prettier**

`web/.eslintrc.cjs`（若装的 eslint 9 只认 flat config，就写成 `eslint.config.js`，以实际能跑通 `npm run lint` 为准，并在报告里说明用的哪种）：

```js
module.exports = {
  root: true,
  extends: [
    'plugin:vue/vue3-recommended',
    '@vue/eslint-config-typescript',
    '@vue/eslint-config-prettier',
  ],
  parserOptions: { ecmaVersion: 'latest' },
  rules: {
    // 组件名允许单个单词：页面组件就叫 Login、Logs，强行凑两个词更难读
    'vue/multi-word-component-names': 'off',
  },
}
```

`web/.prettierrc.json`：

```json
{
  "semi": false,
  "singleQuote": true,
  "printWidth": 100,
  "trailingComma": "all"
}
```

```bash
cd /f/danmuku/web; npm run lint 2>&1 | tail -10; echo "退出码=$?"
```

- [ ] **Step 9: CI 接线**

`.github/workflows/ci.yml` 加一个前端 job。**先读现有文件**，按它的风格写，别覆盖已有 job：

```yaml
  webui:
    name: 前端检查
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: 安装依赖
        working-directory: web
        run: npm ci
      - name: 类型检查
        working-directory: web
        run: npm run typecheck
      - name: Lint
        working-directory: web
        run: npm run lint
      - name: 测试
        working-directory: web
        run: npm run test
      # 构建产物是提交进仓库的。CI 重新构建一次并比对，
      # 防止有人改了前端源码却忘了提交重新构建的产物——
      # 那种情况下线上跑的还是旧界面，而且不报任何错。
      - name: 构建并检查产物是否与仓库一致
        working-directory: web
        run: npm run build
      - name: 产物必须已提交
        run: |
          if ! git diff --quiet -- server/internal/webui/dist; then
            echo "构建产物与仓库中的不一致——改了前端源码后请重新 npm run build 并提交 server/internal/webui/dist" >&2
            git diff --stat -- server/internal/webui/dist >&2
            exit 1
          fi
```

**这道守卫是这个任务里最有价值的一步。** 没有它，「改了前端忘了提交产物」这件事在本地和 CI 都完全不报错，只有部署之后才发现界面是旧的。

- [ ] **Step 10: 提交**

```bash
cd /f/danmuku
git add web/ server/internal/webui/dist/ .github/workflows/ci.yml .gitignore
git commit -m "$(cat <<'EOF'
feat: WebUI 脚手架（Vue3 + TS + Vite + Naive UI）

产物直接输出到 server/internal/webui/dist/，不做中间同步步骤——
多一个步骤就多一处会忘记执行的地方，而忘记的表现是「改了前端却
没生效」，很难查。

CI 加了一道守卫：重新构建后比对 server/internal/webui/dist 有无
差异。没有它，「改了前端源码忘了提交重新构建的产物」在本地和 CI
都完全不报错，只有部署之后才发现界面是旧的。

开发代理刻意用 changeOrigin: false——后端会话 Cookie 是
SameSite=Lax，改写 Origin 会让开发环境的表现与生产不一致，
把本该在开发期暴露的问题藏到上线才发现。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: API 客户端与类型定义

**Files:**
- Create: `web/src/api/client.ts`、`web/src/api/types.ts`、`web/src/api/index.ts`
- Create: `web/src/api/client.test.ts`

**Interfaces:**
- Produces: `request<T>(method, path, body?)`、`ApiError`（带 `status` 与 `message`）、以及全部接口的类型定义

**类型定义手写，不做代码生成。** 后端是 Go，生成 TS 类型要引入 OpenAPI 或 tygo 这类工具链，而接口总共三十来个、字段都很浅。手写一份并让它成为**唯一的类型来源**，比维护一条生成管线便宜。代价是后端改字段时要手动同步——这一点写进 `types.ts` 的顶部注释。

- [ ] **Step 1: 写失败的测试**

`web/src/api/client.test.ts`：

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { ApiError, request } from './client'

function mockFetch(status: number, body: unknown, contentType = 'application/json') {
  return vi.fn().mockResolvedValue(
    new Response(typeof body === 'string' ? body : JSON.stringify(body), {
      status,
      headers: { 'Content-Type': contentType },
    }),
  )
}

describe('request', () => {
  beforeEach(() => {
    vi.unstubAllGlobals()
  })

  it('成功时返回解析后的 JSON', async () => {
    vi.stubGlobal('fetch', mockFetch(200, { name: '小号' }))
    await expect(request<{ name: string }>('GET', '/api/accounts')).resolves.toEqual({
      name: '小号',
    })
  })

  it('带 credentials 发请求——会话是 HttpOnly Cookie，不带就永远是未登录', async () => {
    const f = mockFetch(200, {})
    vi.stubGlobal('fetch', f)
    await request('GET', '/api/auth/me')
    expect(f.mock.calls[0][1]).toMatchObject({ credentials: 'same-origin' })
  })

  it('把后端的 {"error": "..."} 原样抛出来', async () => {
    vi.stubGlobal('fetch', mockFetch(422, { error: '规则 关键词回复 的正则非法' }))
    await expect(request('POST', '/api/x')).rejects.toThrow('规则 关键词回复 的正则非法')
  })

  it('ApiError 带上状态码，调用方才能区分 403 与 404', async () => {
    vi.stubGlobal('fetch', mockFetch(403, { error: '你在 小号@123 上没有 rule:write 权限' }))
    await expect(request('POST', '/api/x')).rejects.toMatchObject({ status: 403 })
  })

  // 后端的 SPA 回退会把未知路径返回成 HTML。若不特判，JSON.parse 抛出的
  // 是「Unexpected token <」，完全指不到真正的原因。
  it('响应不是 JSON 时给出能看懂的错误，而不是 JSON 解析异常', async () => {
    vi.stubGlobal('fetch', mockFetch(200, '<!doctype html><html></html>', 'text/html'))
    await expect(request('GET', '/api/typo')).rejects.toThrow(/不是 JSON/)
  })

  it('204 无响应体时不去解析', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(new Response(null, { status: 204 })))
    await expect(request('DELETE', '/api/x')).resolves.toBeUndefined()
  })

  it('网络层失败时也抛 ApiError，status 为 0', async () => {
    vi.stubGlobal('fetch', vi.fn().mockRejectedValue(new TypeError('Failed to fetch')))
    await expect(request('GET', '/api/x')).rejects.toBeInstanceOf(ApiError)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /f/danmuku/web; npm run test 2>&1 | tail -15; echo "退出码=$?"
```

期望：`Cannot find module './client'`。

- [ ] **Step 3: 实现**

`web/src/api/client.ts`：

```ts
/** ApiError 承载后端返回的中文说明与 HTTP 状态码。 */
export class ApiError extends Error {
  /** HTTP 状态码。网络层失败（根本没连上）时为 0。 */
  readonly status: number

  constructor(status: number, message: string) {
    super(message)
    this.name = 'ApiError'
    this.status = status
  }
}

/** 未登录时的回调，由路由守卫注册。 */
let onUnauthorized: (() => void) | null = null

export function setUnauthorizedHandler(fn: () => void) {
  onUnauthorized = fn
}

export type Method = 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE'

/**
 * request 是前端访问后端的唯一出口。
 *
 * 所有错误都归一成 ApiError：调用方只需要 catch 一种东西，
 * 并且总能拿到一句可以直接显示给用户的中文说明。
 */
export async function request<T = void>(method: Method, path: string, body?: unknown): Promise<T> {
  let resp: Response
  try {
    resp = await fetch(path, {
      method,
      // 会话是 HttpOnly Cookie。不带 credentials 的话每个请求都是未登录，
      // 而表现是「登录成功后立刻又被踢回登录页」，很难查。
      credentials: 'same-origin',
      headers: body === undefined ? {} : { 'Content-Type': 'application/json' },
      body: body === undefined ? undefined : JSON.stringify(body),
    })
  } catch (e) {
    // 网络层失败：后端没起、断网、被防火墙挡住
    throw new ApiError(0, `连不上服务端：${e instanceof Error ? e.message : String(e)}`)
  }

  if (resp.status === 401) {
    onUnauthorized?.()
    throw new ApiError(401, '登录已过期，请重新登录')
  }

  if (resp.status === 204 || resp.headers.get('Content-Length') === '0') {
    return undefined as T
  }

  const contentType = resp.headers.get('Content-Type') ?? ''
  if (!contentType.includes('application/json')) {
    // 后端的 SPA 回退会把未知路径返回成 HTML。不特判的话，
    // JSON.parse 抛的是「Unexpected token <」，完全指不到真正的原因。
    throw new ApiError(resp.status, `响应不是 JSON（Content-Type: ${contentType || '空'}），` +
      `多半是请求路径写错了：${method} ${path}`)
  }

  let data: unknown
  try {
    data = await resp.json()
  } catch {
    throw new ApiError(resp.status, `响应不是合法 JSON：${method} ${path}`)
  }

  if (!resp.ok) {
    const msg =
      typeof data === 'object' && data !== null && 'error' in data
        ? String((data as { error: unknown }).error)
        : `请求失败（HTTP ${resp.status}）`
    throw new ApiError(resp.status, msg)
  }

  return data as T
}
```

- [ ] **Step 4: 类型定义**

`web/src/api/types.ts`。**顶部必须有这段注释**：

```ts
// 后端接口的类型定义，手写维护。
//
// 不做代码生成：后端是 Go，生成 TS 要引入 OpenAPI 或 tygo 这类工具链，
// 而接口总共三十来个、字段都很浅，维护一条生成管线比手写贵。
//
// **代价是后端改字段时要手动同步这里。** 改后端的人不会自动想到改这里，
// 所以两处不一致的表现是运行期 undefined，而不是编译错误。
// 后端各个 *View 结构体（accountView / bindingView / ruleView /
// memberView / blockedView / activityView）是权威，以它们为准。

/** 六个权限点。与后端 internal/perm 的常量一一对应。 */
export type Permission =
  | 'rule:read'
  | 'rule:write'
  | 'danmaku:send'
  | 'user:block'
  | 'member:manage'
  | 'event:read'

export interface User {
  username: string
  isAdmin: boolean
  createdAt: string
}

export interface Account {
  name: string
  uid: string
  rateLimitMs: number
  maxLength: number
  ownerId: number
  createdAt: string
  // 注意：后端刻意不返回 cookie 字段。前端不该有任何地方去接它。
}

export interface Binding {
  id: number
  accountId: number
  accountName: string
  roomId: string
  enabled: boolean
  ruleCount: number
  /** 调用者在这个绑定上拥有的权限点，前端据此决定显示哪些按钮。 */
  permissions: Permission[]
}

export interface BlockedUser {
  id: number
  uid: string
  username: string
  reason: string
  createdBy: number | null
  createdAt: string
}

export type ActivityKind = 'event' | 'action'

export interface Activity {
  id: number
  kind: ActivityKind
  eventType: string
  actionType: string
  ruleName: string
  userUid: string
  userName: string
  detail: unknown
  occurredAt: string
}

export interface Member {
  username: string
  permissions: Permission[]
}
```

**规则的类型（`spec.Rule` 对应物）留到 Task 9**，那时才真正用到，现在写只会写歪。

- [ ] **Step 5: 跑测试确认通过、检查、提交**

```bash
cd /f/danmuku/web; npm run test 2>&1 | tail -15; echo "退出码=$?"
cd /f/danmuku/web; npm run typecheck 2>&1 | tail -5; echo "退出码=$?"
cd /f/danmuku/web; npm run lint 2>&1 | tail -5; echo "退出码=$?"

cd /f/danmuku
git add web/src/api/
git commit -m "$(cat <<'EOF'
feat: WebUI 的 API 客户端与类型定义

request 是前端访问后端的唯一出口，所有错误归一成 ApiError——调用方
只需要 catch 一种东西，并且总能拿到一句可以直接显示给用户的中文说明。

两处刻意的特判：

一、Content-Type 不是 JSON 时给出能看懂的错误。后端的 SPA 回退会把
未知路径返回成 HTML，不特判的话 JSON.parse 抛的是「Unexpected token <」，
完全指不到真正的原因（路径写错了）。

二、credentials: 'same-origin' 不能省。会话是 HttpOnly Cookie，不带的话
每个请求都是未登录，而表现是「登录成功后立刻又被踢回登录页」。

类型定义手写不做生成，理由与代价都写进了 types.ts 的顶部注释。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```
